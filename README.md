This project implements and benchmarks an optimized image embossing filter using x86 MMX instructions, with execution time measured in CPU cycles via the RDTSC instruction.

### Build & Run
```bash
make
./filter_png image_name.png
